services:
  mysql:
    env_file:
      - .env.prod
    restart: unless-stopped
    ports:
      - "127.0.0.1:3306:3306"

  redis:
    command: redis-server --requirepass ${REDIS_PASSWORD}
    env_file:
      - .env.prod
    restart: unless-stopped
    ports:
      - "127.0.0.1:6379:6379"